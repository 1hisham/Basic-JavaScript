const obj1 ={
    name : 'codemalayalm',
    place: 'kerala',
    rank:100
};

function myFunction(){
    const name = obj1.name;
    const place = obj1.place;
    const rank = obj1.rank
    console.log(name,place,rank);
}

myFunction();
